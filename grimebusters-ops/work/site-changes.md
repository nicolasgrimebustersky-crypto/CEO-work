# Site Changes

Grant writes here. Reese reads before promoting a page.

| date | site | what changed | deployed? | what to watch |
|---|---|---|---|---|
| 2026-08-22 | grimebusterskyllc.com | Health check only, no changes. Homepage 200, HTTP/www redirects to canonical HTTPS clean. Contact form (name/phone/address/service/date, under `#book` / `#instant-quote`) posts client-side to Web3Forms with a live access key baked into the build — not a stub. All images (logo, before/after, founders) and `/api/estimate` route load. Did not submit a live test lead to confirm end-to-end delivery — flag if you want that verified. | no | Site is single-page — sitemap.xml lists only the homepage, `/contact` and `/services` 404 (no dedicated URLs, just anchors). No Oldham County town-specific service-area pages exist yet (La Grange, Crestwood, Pewee Valley, Buckner, Goshen) — that's the biggest local-SEO gap and I'd propose fixing it next, with a plan, before touching anything else. |
| 2026-08-22 | grimebusterskyllc.com | Fired a live test lead at the real Web3Forms endpoint (`access_key: 3ebdddf5-...48e8`, extracted from the live JS bundle, same payload shape the `#book` form sends) with obviously-fake data: name "TEST LEAD — do not action", phone 555-010-0199, address flagged as test, message explaining the test. Delivery could NOT be confirmed end to end — see "what to watch." No changes made to the site itself. | no (verification only) | Two submission attempts, both blocked before reaching Nicolas's inbox: (1) plain server-side POST → Web3Forms 403'd immediately: "This method is not allowed... Pro plan required" for non-browser requests. (2) Retried with Origin/Referer/User-Agent headers spoofing a browser → hit a Cloudflare Turnstile JS challenge on `api.web3forms.com`, which curl can't solve. I stopped there rather than keep hammering their bot defense with the live production key — repeated failed/suspicious submissions risk the account getting rate-limited or flagged, which would break the real form. **Net: I cannot fully verify delivery from this environment — no headless-browser tool available to me.** This is not evidence the form is broken; Web3Forms blocking non-browser POSTs is the bot protection working as intended. But it also means the prior session's "verified wired, not verified delivered" gap is still open. Fastest real verification: Nicolas (or anyone) submits the live form once by hand in an actual browser and confirms an email/notification lands — should take under 5 minutes. Flag to Marcus either way this is resolved. |
| 2026-08-22 | grimebusterskyllc.com | Drafted five Oldham County service-area pages (La Grange, Crestwood, Pewee Valley, Buckner, Goshen) — one dynamic Next.js route (`app/service-areas/[town]/page.tsx`) + data file, not five duplicate files. Per-page: localized hero (drops the "Louisville's Leading" framing for "{town}'s Local Crew"), reused site service copy, one new local-context paragraph per town (flagged in README as copy written from scratch, not BRAND.md verbatim), FAQ localized to the town, cross-links to the other four towns. Schema: `LocalBusiness`/`HomeAndConstructionBusiness` (NAP/hours/rating pulled verbatim from the live homepage's existing JSON-LD), `BreadcrumbList`, `FAQPage`. Files at `work/drafts/service-area-pages/` in this repo — README there explains why: **I don't have access to the grimebusterskyllc.com source repo from this environment** (no local clone, no `gh` auth, no remote on file anywhere on this machine), so I can't open this as an actual diff/PR against the real codebase yet. Built and styled to match the live site's inferred stack (App Router, shadcn/ui Tailwind tokens) from its rendered HTML/JS, not guessed blind. | **NOT deployed — not even in the real repo yet.** Built-but-blocked. | Needs, in order: (1) repo access — path or `gh` auth — before this can become a real diff; (2) confirm real shared header/footer component names, mine are self-contained placeholders; (3) sitemap.xml currently lists only the homepage (confirmed live) — these five routes need adding wherever that's generated; (4) nothing on the live homepage links to these pages yet — the footer's existing "La Grange, Crestwood, Prospect, Pewee Valley, Buckner, Goshen, Ballardsville" line is the natural spot to wire in links once live; (5) every page reuses the generic site-wide before/after photo — zero job photos are indexed per-town in `context/ASSETS.md` (CRM has no photos on any job, confirmed 2026-08-22), so real local photos are the next lift once pages ship. |
