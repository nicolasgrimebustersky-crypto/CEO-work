# Site Changes

Grant writes here. Reese reads before promoting a page.

| date | site | what changed | deployed? | what to watch |
|---|---|---|---|---|
| 2026-08-22 | grimebusterskyllc.com | Health check only, no changes. Homepage 200, HTTP/www redirects to canonical HTTPS clean. Contact form (name/phone/address/service/date, under `#book` / `#instant-quote`) posts client-side to Web3Forms with a live access key baked into the build — not a stub. All images (logo, before/after, founders) and `/api/estimate` route load. Did not submit a live test lead to confirm end-to-end delivery — flag if you want that verified. | no | Site is single-page — sitemap.xml lists only the homepage, `/contact` and `/services` 404 (no dedicated URLs, just anchors). No Oldham County town-specific service-area pages exist yet (La Grange, Crestwood, Pewee Valley, Buckner, Goshen) — that's the biggest local-SEO gap and I'd propose fixing it next, with a plan, before touching anything else. |
