# Telegram Inbox

Replies Nicolas sends to the bot land here via `scripts/check-replies.sh`.
Marcus reads this at the start of every session.

This is a queue, not a live conversation — a reply sent at noon gets acted on
at the next session or the next scheduled run.

Marcus marks items `[handled]` once acted on.
