# Copy to .env and fill in. NEVER commit .env.
TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=

# Firestore (GrimelineCRM), read-only service account
GOOGLE_APPLICATION_CREDENTIALS=./firebase-readonly.json
FIREBASE_PROJECT_ID=
